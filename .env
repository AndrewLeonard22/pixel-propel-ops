# ⛔ THIS FILE IS DELIBERATELY TRACKED — see .gitignore. Vite compiles these three values
# into the client bundle, so untracking it once deployed the app with NO database URL.
# They are the PUBLIC project ref and PUBLISHABLE (anon) key. No secret belongs here.
#
# 🔴 CORRECTED 2026-08-11. These pointed at project `tclghhfozyfsdkqyaftc`, which is NOT
# where this app`s data lives. Measured against it with this same anon key:
#     GET /rest/v1/ad_insights   -> 200  []
#     GET /rest/v1/app_settings  -> 200  []
# A perfectly successful conversation with the wrong database. Every guard then agreed —
# the fetch "succeeded", completeness read 0 fetched === 0 expected, and the dashboard
# rendered $0.00 on every tile with a green source badge. That is a 100% data loss shown
# as health, and it would have landed on the exact commit that moved ad spend ONTO
# Supabase — the cutover reporting zero instead of the $770k it exists to reveal.
#
# `mlwoztsytapxjgfldyzv` is where ad_insights / ad_accounts / app_settings actually live,
# and it is what the LIVE bundle at adsdata.socialworkspro.com already serves: the key
# below is byte-identical to the one measured in that deployed bundle, so this file now
# reproduces production rather than contradicting it.
VITE_SUPABASE_PROJECT_ID="mlwoztsytapxjgfldyzv"
VITE_SUPABASE_PUBLISHABLE_KEY="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im1sd296dHN5dGFweGpnZmxkeXp2Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODY0OTA0ODQsImV4cCI6MjEwMjA2NjQ4NH0.0CjBGmmKZ0h4ze7ud6Ojm6RnWSaTy-mlMK7WNfx_xLk"
VITE_SUPABASE_URL="https://mlwoztsytapxjgfldyzv.supabase.co"
